// login_screen.dart
import 'package:flutter/material.dart';

class GeolocalisationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Se géolocaliser'),
      ),
      // Ajoutez le contenu de votre écran de connexion ici
    );
  }
}